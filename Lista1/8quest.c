#include <stdio.h>
#include <math.h>

int main () {

  int number, result;

  scanf("%d", &number);
  printf("Digite um número: %d\n", number);

  result= number * number;
  printf("Resultado da conta: %d\n", result);

  return 0;
}